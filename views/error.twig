{% extends "./layout/layout_cover.twig" %}

{% block content %} 
    <div class="inner cover">
        <h1 class="cover-heading">{{title}}</h1>
        <p class="lead">{{ message | raw }}</p>
        <p class="lead">
          {{ action | raw }}
        </p>
    </div>
{% endblock %}