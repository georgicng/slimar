{% extends "./layout/layout.twig" %} {% block content %}
<div class="content">
    {% if (page) %}
        {{ page | raw }}
    {% else %}
        <div class="alert alert-warning"> Page not found </div>
    {% endif %}
</div>
{% endblock %}