{% extends "./layout/layout.twig" %} {% block content %}
<div class="row">
    {% for user in users %}
    <a href="profile.php?u={{user['bio']['username']}}">
        <div class="col-xs-4 col-sm-1  col-md-3">
            <div class="thumbnail">
                <img src="{{user['dp']}}" style="width:150px;height:150px;" alt="...">
                <div class="hidden-sm hidden-xs ">
                    <div class="text align-center" style="Font-size:14pt;">
                        <font style="{{user['perm']['css']}}">{{user['bio']['username']}}</font>
                    </div>
                </div>
            </div>
        </div>
    </a>
    {% endfor %}
</div>
{% endblock %}